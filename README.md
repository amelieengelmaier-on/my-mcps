# my-mcps

Personal MCP servers.

## tempo-mcp

Tempo time tracking MCP — log, view, edit, and delete Tempo worklogs from opencode.

### Setup

```bash
cd tempo-mcp
cp .env.example .env
# fill in TEMPO_API_TOKEN and TEMPO_ACCOUNT_ID in .env
npm install
```
